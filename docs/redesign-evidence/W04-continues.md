# W04 — Actual wipe and persisted checkpoint continue

The diagnostic room now reaches a wipe through real player falls, confirms its last input journal segment, and accepts a signed host continue. The same SQLite transaction spends the credit, reconstructs enemies with fresh IDs, resets player entry state and replaces input generations. [Archive format 3 contract](../combat-checkpoint-v3.md). This is one diagnostic checkpoint; authored missions, terminal result/outbox publication, rematch and the production v3 room remain unfinished.

## Behavior and verification

The wipe waits for the last death presentation. The room publishes the nonterminal input pause immediately and keeps continue locked until storage confirms. Packets already in flight drain without applying commands or renewing a lease; their original packet fingerprints and delivery checks remain enforced. Input after acknowledging the pause remains invalid. Clients acknowledge the replacement loading baseline immediately and prepare a fresh input cohort before the current host starts it.

Authoritative life transitions now restore player state during reconciliation. Death, protected entry and spectating keep their existing input/ownership generation; run, connection, control, body, geometry and vehicle changes remain fenced. Rebuilt checkpoint enemies activate from the pending encounter roster rather than world tick zero.

| Verification                         | Result                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CHECKPOINT_DISABLE=1 pnpm check`    | 456 tests in 46 files; lint, asset/content validation, all TypeScript targets, client/Worker dry-run and CDKTN synthesis pass. [Log](W04-continue-check.log.gz).                                                                                                                                                                                                                                |
| `pnpm test:contracts`                | Node, Chromium and local workerd agree. Actual wipe at tick 523; continued epoch 2 with enemy IDs 1217/1218; checkpoint hash `d0048e3b`; replay/reconciliation through tick 538 hash `a2091c26`. [Report](W04-continue-contracts.json.gz).                                                                                                                                                      |
| `pnpm test:storage:combat`           | Fresh workerd processes preserve wipe, continued entry and credits. Transaction rollback, a host changing during encoding, stale prefix retry and a lost response after confirmed commit cannot spend twice. Recovery advances to epoch 3 without another credit. [Report](W04-continue-storage.json.gz).                                                                                       |
| `--combat-campaign` browser test     | Four isolated Chromium contexts reach wipe at tick 528 and hold its last write with an 18-tick backlog. All four reconnect in the same documents at epoch 2, then agree on 15 resumed snapshots through tick 573; final room observation is tick 578. [Report](W04-continue-browser.json.gz), [diagnostics](W04-continue-diagnostics.json.gz), [inspected screenshot](W04-continued-party.png). |
| `--combat-phases` regression         | Lobby/host/loading/playing transitions pass with 18 common snapshots through tick 252. [Report](W04-continue-phases.json.gz), [diagnostics](W04-continue-phase-diagnostics.json.gz).                                                                                                                                                                                                            |
| `--combat-auto-reconnect` regression | Empty tick 170 survives process restart; 15 common resumed snapshots agree. The real expiry alarm fires once at stopped tick 290; expiry observation takes approximately 98.2 seconds. [Report](W04-continue-auto.json.gz), [diagnostics](W04-continue-auto-diagnostics.json.gz).                                                                                                               |

The browser test rejects continue before confirmation (409), unsigned commands (401), non-host commands (403), load bypass and stale generations (409), and a newcomer during a wipe (409, `in-progress`). Host succession during reconnect is permitted and the old host is checked against its resulting role. The pure test exhausts three actual checkpoint retries into defeat and rejects another load/continue; it does not publish a terminal result.

## Retained failures and limits

- The first multi-continue fixture exposed tick-zero-only enemy activation: rebuilt targets remained pending and could not resolve. [Failure](W04-continue-activation-failure.log.gz).
- Browser attempt 1 exposed the immutable-life prediction check: first death disconnected the cohort. [Failure](W04-continue-attempt-1-failure.json.gz), [clients](W04-continue-attempt-1-failed-clients.json.gz).
- Attempt 2 reached the wipe but rejected pre-pause packets already in flight. Its write-hold predicate also incorrectly included spectating phase timestamps. Both were corrected. [Failure](W04-continue-attempt-2-failure.json.gz), [clients](W04-continue-attempt-2-failed-clients.json.gz).
- Attempts 3 and 4 stopped on runtime clock regressions of 840 ms at tick 96 and 768 ms at tick 213. Host wall time also moved backward while monotonic time advanced. [Attempt 3](W04-continue-attempt-3-failure.json.gz), [host samples](W04-continue-attempt-3-diagnostics.json.gz), [attempt 4](W04-continue-attempt-4-failure.json.gz), [host samples](W04-continue-attempt-4-diagnostics.json.gz).
- Attempt 5 completed continue but found missing loading-baseline acknowledgments. [Failure](W04-continue-attempt-5-failure.json.gz). Attempt 6 found a verifier assumption that the same host retained its role through reconnect; role and generation denials are now checked separately. [Failure](W04-continue-attempt-6-failure.json.gz).
- The first automatic-reconnect regression stopped on a clock regression at tick 267; the following run passed with unchanged guards. [Failure](W04-continue-auto-attempt-1-failure.json.gz), [clients](W04-continue-auto-attempt-1-failed-clients.json.gz), [host samples](W04-continue-auto-attempt-1-diagnostics.json.gz).

The host identifies as WSL2. A read-only time-service observation reported NTP enabled/synchronized with an approximately −1.87-second offset. This adds context but does not establish the underlying clock correction source. The six-tick input lead, scheduling debt, catch-up, watchdog and thirty-tick persistence limits remain unchanged. These passes do not close long-running timing or deployed cadence gates.

Wrangler source and generated deployment configuration enable traces, invocation logs and 100% log/trace sampling, plus source-map upload. [Configuration evidence](W04-continue-observability.json). A read-only Cloudflare lookup succeeded: the configured `edgefall` Worker returned 404/code 10007, and the accessible account's Worker list contained no Edgefall-named deployment. [Sanitized lookup](W04-continue-cloudflare-read.json). No resource changed and live ingestion remains unverified. [Observability context](observability.md).

Reports record the pre-commit base revision. The [artifact/source manifest](W04-continue-artifacts.json) pins delivered source bytes and retained artifacts; output bundle hashes are in the reports. Failed attempts describe earlier local iterations and are not claimed to match final source. The full redesign and human art/playtest gates remain open.
