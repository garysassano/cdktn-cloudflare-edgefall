# Kestrel tread, suspension and recoil candidate

Kestrel now renders its tracks, hull, turret and hatch/crew as four independent native layers at the accepted body root. Eight tread exposures replace the former four combined chassis frames. The clock advances from the solver's accepted ground velocity, which excludes platform carry and is clipped by walls. Stopping retains the current tread phase; airborne tracks hold that phase in an extended drawing. This is a cosmetic stride clock, not a measurement of every partial displacement consumed before a collision. The hull has separate loaded and returning suspension drawings, so driving through an impact does not restart the tracks.

Accepted motion selects two drawings each for launch, rise, apex and fall. A launch begins after the actual jump impulse, with no input delay. Ceiling contact enters descent immediately, and a short fall does not invent launch or apex acting. Coyote and buffered jumps can begin a launch without an intervening grounded snapshot. Three-tick light and five-tick heavy impact clips follow real airborne-to-grounded transitions. Heavy classification uses the previous downward velocity at four logical pixels per tick; it does not model relative collision impulse or change damage. Tracks remain at the floor during impact. Movement, aim and fire continue independently; a new jump immediately cancels compression. Seat epoch changes preserve the physical chassis clock, while a new body or wreck resets it.

The roof's central attachment stays fixed while the nose, rear armor and exposed springs articulate. The eight world turret headings remain independent of hull reflection. Each accepted release uses the retained full-length barrel and original muzzle socket. The following tick selects a new two-pixel kick drawing, then the retained one-pixel recovery drawing before the action returns to ready. No simulation/controller/content/codec/binding change or runtime figure generation is involved.

The editable source contains 72 original drawings and 288 palette entries: 39 new drawings replace six former combined hull/tread and air/landing drawings, and 33 turret, crew, hatch and wreck drawings remain exactly equal to `ccf416607dafa9ea42793dfc59ddd5c3d005b2da`. The curator independently verifies that preservation, the palettes, canvas, release sockets and both seat clips. The full native inventory is now 404 originals and 1,067 atlas entries. Kestrel's atlas is 1500×2000, using 12,000,000 decoded RGBA bytes; the seven native textures total 34,169,600 bytes. The existing 2048-pixel dimension and 64 MiB decoded texture limits remain unchanged. GPU allocation and mipmap overhead are not measured by that count. Export auditing now derives its grid columns from the actual packed dimensions instead of assuming eight columns.

The [review manifest](style-v2/kestrel-motion/artifacts.json) pins 344 artifacts, 533 source fingerprints and 13,086,336 artifact bytes. Its SHA256 is `252e35e052668941d81ae01655919f39d77bc54911332a2b6b08a41ebfc43883`. Twenty-four composed sheets show [treads](style-v2/kestrel-motion/sheets/drive-white-1x.png), [airborne suspension](style-v2/kestrel-motion/sheets/air-white-4x.png), [impact](style-v2/kestrel-motion/sheets/impact-white-4x.png) and [all aim/recoil exposures](style-v2/kestrel-motion/sheets/aim-white-4x.png) against black, white and chroma backgrounds at native and 4× nearest-neighbour sizes. Component sheets retain every current cast drawing. Every new drawing appears in a composed source sheet; browser captures cover 34 of the 39 new drawings. Extended tread phases 0, 1, 4, 6 and 7 have source-sheet coverage only.

Six actual browser studies pass 815 accepted world/presentation boundaries, 54 tank shot releases matched to their drawn muzzles, 120 pixel captures, and recording replay/import restoration. The captures compare 228,507 visible logical sprite pixels and every corresponding scaled output pixel. The inspector's effect/projectile overlays and floor-clipped area are excluded from source-pixel comparisons; the current harness does not count those exclusions. The two new 180-tick keyboard routes drive, reverse, jump, sweep all eight headings while firing, land and jump again. They cover all 15 hull drawings, all eight ground tread drawings and all 24 turret release/kick/recovery drawings in each route. The right route also receives a real destruction-driven ejection at tick 167. The pre-existing rifle, shield, boarding/ordinary-exit and four-player cases remain in the same run.

The [right quarter-speed movie](style-v2/kestrel-motion/cast/kestrel-right-clean-quarter.mp4) and [left quarter-speed movie](style-v2/kestrel-motion/cast/kestrel-left-clean-quarter.mp4) retain the complete sequences. Clean normal and debug normal recordings accompany each; the package also retains the shield and ordinary tank/crew movies, for 12 MP4s and their 12 raw WebM captures. New study movies last 3.10–3.12 seconds at normal speed and 12.15–12.17 seconds at quarter speed, including capture lead-in. Each real playback finishes at the same accepted state/presentation with no rejected audio voices. Separate stereo audits of raw and transcoded audio pass with a maximum channel peak of −12.20 dBFS at the inspector's existing volume. Chromium uses `--disable-audio-output` with WebAudio and MediaRecorder active; physical output and human listening acceptance remain unverified.

Current solo, two-player and four-player mission replays pass 9,202 noninitial world/effects boundaries, 72 selected hero/cast boundaries and 144 clean/debug stills. The curator compares all 9,205 recorded world/effects hashes, including the three initial states, against the published operative-air baseline and requires exact equality. Every player survives; forced ejection remains at ticks 1980, 2003 and 2357 with 12 protection ticks. The current recordings and stills are retained under `missions/`. Continuous mission movies remain at the historical crew art revision; this increment does not relabel them as current art.

`CHECKPOINT_DISABLE=1 pnpm check` passes 694 tests across 69 files, source/asset/audio validation, type checking, the production client/Worker dry run and CDK Terrain synthesis. Twenty-seven new tests exercise actual wall and ceiling contacts, moving support, short falls, eight tread phases in either direction, immediate drive/jump response, coyote/buffered launches, identity/seat/wreck policy and all eight firing headings with either hull facing. The required crew regression also passes. No binding shape or depicted service flow changed.

Reproduce with `pnpm art:export`, `pnpm assets:validate`, `node scripts/build-client.mjs --lab`, `pnpm tsx scripts/verify-native-cast.mjs`, and `node scripts/render-kestrel-review.mjs`. Run `pnpm tsx scripts/verify-breakwater.mjs --players=N --no-video` for N=1, 2 and 4 before `node scripts/curate-kestrel-evidence.mjs`. The curator refuses an existing destination. Set `EDGEFALL_CHROMIUM_PATH` when using an externally installed Chromium.

The next W06 work is the complete current solo/two/four clean/debug normal/quarter music-on/off and reduced-effects mission movie matrix, followed by physical-device and human style/listening review. This remains a benchmark candidate. Production v3 integration, full weapon/vehicle/campaign coverage and live trace ingestion are still open. Both source and generated Wrangler configurations retain explicit traces, invocation logs, full sampling and source maps, consistent with [Cloudflare's trace configuration](https://developers.cloudflare.com/workers/observability/traces/); no live-ingestion or deployment claim is made here.
