# Breakwater interaction audio candidate

The benchmark now uses 52 recorded cue and loop variants with explicit voice priorities, spatial ranges and cooldowns. Sidearm, HMG, rifle, shotgun and tank gun have different layer arrangements; boarding, ordinary exit, emergency ejection, armor hits and boss warnings have separate cues. Enemy deaths no longer compete with player damage at the same priority. The [editable session and exact licensed sources](../../audio/source/sfx/README.md) retain the reconstruction path and lossless derived masters.

The runtime set occupies 383,694 compressed bytes and 3,762,240 decoded mono bytes. Combined with both authored music arrangements, the current encounter uses 24,242,240 decoded audio bytes, below the 32 MiB proposal. A second source render reproduces all 106 master/runtime/manifest files byte-for-byte with the installed toolchain. Browser decoding matches every declared frame count; the engine and flame loops each repeat three times through `OfflineAudioContext` with measured seams below 0.003.

## State and lifetime

Engine playback derives from occupied, living, connected ownership with the matching control epoch. Flame playback derives from attached, uncancelled accepted emissions; detached visual lobes do not keep the emitter roaring. The renderer reconciles current presence without replaying historical ignition cues. Existing engine sources survive repeated reconciliation and adjust pitch with accepted movement. Pause, audio disable, accepted exit/ejection, scene completion, replay reset and disposal stop persistent playback. Disposing during capture settles the pending capture promise, and decoding completion cannot repopulate disposed buffers.

Accepted cues carry stable action/event identity within the current playback epoch. Duplicate accepted ejection is played once. All 52 samples preload before playback; trigger and lifecycle checks issue no sound-file requests. The diagnostic selectors can omit disconnected owners, while production membership and prediction/confirmation integration remain part of W04.

The current solo mission's late tank sequence is a **voluntary exit**, begun at tick 1954 and detached at 1961, before the tank becomes a wreck. The earlier screenshot label `ejection` is not proof of forced ejection. The separate accepted four-tank damage fixture ejects P4 at tick 183 and now verifies the emergency release, tank destruction and engine stop together. The continuous benchmark's remaining acting/review inventory must retain this distinction.

## Voice pressure and output

The mixer has 32 audible SFX slots, including at most eight persistent loops, with per-cue caps and higher priority for player damage, boarding and boss warnings. It virtualizes quiet or distant loops first, then admits them again from current state when space returns. A stolen slot fades for 5 ms before its replacement starts. The stress capture observed 32 audible voices and 36 graph nodes during replacement; the graph has a separate 64-node transition bound. The stress combines four accepted engine states and four accepted flame states to exercise the declared maximum mixer capacity; it does not claim that the current mission permits one player to drive and use an on-foot flame weapon simultaneously.

A full-volume capture exposed a signal problem beyond the voice count: decoded peaks reached 1.093/1.084. The shared scene output now passes both music and SFX through a four-times-oversampled smooth overload knee. Signals up to 0.7 remain linear; the curve approaches 0.85 above that. The final full-volume capture, including music, peaks at 0.929/0.885 after Opus decoding. Both stereo channels must remain below 0.99 in the harness. This guard limits overload; it does not establish perceived mix quality.

The comparison contains 23 cue families and 28 actual releases, including six HMG rounds. It retains every release with no cooldown coalescing or dropped voices. Its labels and timing are retained for review. Blind recognition, listening fatigue, physical-device output, background/output-device recovery and the full clean/debug normal/quarter music-on/off review matrix remain open.

## Review artifacts

The [artifact manifest](style-v2/mission-audio/artifacts.json) pins 77 files and 520 source fingerprints. It includes [audio lifecycle/priority evidence](style-v2/mission-audio/audio-report.json), the [23-family cue comparison](style-v2/mission-audio/weapon-comparison.webm), [full-volume mixed audio](style-v2/mission-audio/maximum-mix.webm), [output-overload diagnosis](style-v2/mission-audio/output-diagnosis.json), [short-Ogg controls](style-v2/mission-audio/ogg-diagnosis.json) and [byte-exact second-render evidence](style-v2/mission-audio/reproducibility.json).

Fresh four-player captures cover [clean normal](style-v2/mission-audio/coop-4/coop-4-clean-normal.mp4), [debug normal](style-v2/mission-audio/coop-4/coop-4-debug-normal.mp4) and [clean quarter speed](style-v2/mission-audio/coop-4/coop-4-clean-quarter.mp4). They last 56.45, 56.43 and 224.30 seconds including cosmetic postroll. Each plays 1,906 cues with no rejected voices; the normal mix peaks at 25 slots and quarter speed at 14. Distant inaudible sounds are intentionally virtualized; the legacy `droppedExamples` inspection field also records those decisions with their explicit reason, while `audioDropped` counts missing-buffer/priority failures. The [independent stereo audit](style-v2/mission-audio/mission-stereo-audit.json) passes all six raw WebM and published MP4 files.

Current solo, two-player and four-player replays agree on all 9,202 accepted world and visual boundaries. Their [solo](style-v2/mission-audio/solo/report.json.gz), [two-player](style-v2/mission-audio/coop-2/report.json.gz) and [four-player](style-v2/mission-audio/coop-4/report.json.gz) reports retain 54 screenshots and all three input recordings. This increment records new full videos for four players; earlier solo/two-player videos remain unchanged in the previous evidence folder. Music control/loop checks and the [cast regression](style-v2/mission-audio/cast-regression.json.gz) also pass. The complete music-on/off and debug-quarter matrix still needs current captures after the remaining acting work.

## Validation

`CHECKPOINT_DISABLE=1 pnpm check` passes 638 tests across 66 files, including asset/source/audio validation, type checking, the production client/Worker dry run and infrastructure synthesis. New behavioral coverage includes authority isolation across the complete mission, four-engine/four-flame reconstruction, cancellation, control-epoch loss, actual forced ejection, cue distinction and voice admission under pressure. `pnpm test:audio:combat` exercises the actual WebAudio graph and MediaRecorder in Chromium, with source and artifact fingerprints retained in the evidence package.

The browser explicitly uses `--disable-audio-output`; previous isolated WSL controls found device-clock stalls. No host settings were changed. The running graph and captured signal provide browser-rendering evidence, while actual speakers/controllers/devices and human style/listening approval remain unverified. W06, the broader redesign and deployed trace ingestion remain open.
