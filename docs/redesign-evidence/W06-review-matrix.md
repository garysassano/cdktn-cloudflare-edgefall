# Complete current mission review matrix

The W06 review matrix covers three party sizes and all combinations of four presentation controls: normal/quarter playback, clean/debug overlays, music enabled/muted and full/reduced effects. That is 16 movies per party and 48 overall. Every combination uses the same accepted seeded mission recording for that party. Reduced effects remain a presentation option; movement, combat, damage and outcomes remain authoritative and unchanged.

The inspector now separates preparing a replay from running it. The verifier prepares tick zero, starts canvas/WebAudio capture, paints and retains the initial frame, confirms the world is still at tick zero, then starts playback. It waits for playback to enter the running state before waiting for completion. This avoids both a missed beginning and an asynchronous audio-unlock race. The original play-recording control uses the same preparation function.

`pnpm tsx scripts/verify-breakwater.mjs --players=N --all-videos` writes the complete matrix to `dist/breakwater-matrix/solo`, `coop-2` or `coop-4`. Set `EDGEFALL_CHROMIUM_PATH` for an externally installed Chromium, and build the inspector first with `node scripts/build-client.mjs --lab`. Capture jobs run serially on the WSL host so other browser/build/encoding work cannot disturb their presentation clock. Normal/quarter movies use actual playback with active WebAudio; quarter-speed media is not produced by retiming a normal-speed movie.

All 48 captures passed. The [immutable package](style-v2/mission-review/artifacts.json) retains 399 repository artifacts totaling 7,369,885 bytes, 536 source fingerprints and references to the current operative, Kestrel and audio evidence. Its manifest SHA-256 is `5a5e2508a8b7a814a076d7d74f2b3d8dafc20c309c68f598210a725dd6063f38`. The 48 MP4 and 48 raw WebM files total 778,035,584 bytes. Those movies, six weapon sheets, the offline viewer and the manifest form 104 assets in the [GitHub draft `w06-review-v2-kestrel`](https://github.com/garysassano/cdktn-cloudflare-edgefall/releases/tag/untagged-041ea219d6f27455e6aa), targeting source commit `edac319fdd69d075caf3e1bf877929ad984b9cd9`. Every remote asset's metadata and downloaded bytes match the expected SHA-256 and size. The [delivery receipt](review-matrix-delivery/README.md) retains the complete proof and download instructions. Human acceptance remains pending. [GitHub release assets](https://docs.github.com/en/repositories/releasing-projects-on-github/about-releases) support these individual file sizes.

The report records progress after each completed movie. `--resume` verifies client, runtime-manifest, verifier, video-exporter and input-route identity before reusing results. Each completed movie has hashes for its raw WebM, transcoded MP4, clock report and initial PNG. A cache mismatch fails instead of silently accepting older evidence. Unfinished combinations are recorded again after a stopped process has been confirmed terminal. A live process must be polled or inspected rather than restarted merely because an observation timed out.

Every successful playback must reach the expected final authoritative state, retire terminal music voices, stay within decoded audio and voice budgets, reject no audible cue, and keep its audio and monotonic clocks aligned. Raw and transcoded stereo channels undergo independent headroom checks. Reports retain wall-clock disagreement as a host diagnostic rather than treating wall time as the playback clock. The capture report is complete only after all requested modes are present without duplicates.

The matrix's still-image pass retains 96 captures per party: 24 selected accepted boundaries, each clean/debug and full/reduced. Besides the existing world, effects-history and hero/cast checks, the verifier compares actual reduced-effect drawings against the pure presentation function and confirms that changing the controls leaves the world unchanged. Rendered opacity can be fractional, so drawing fingerprints use presentation JSON; authoritative world fingerprints continue to use the integer-only canonical serializer.

The three runs preserve all 9,202 noninitial world/effects boundaries from the [Kestrel increment](W06-kestrel-motion.md), or 9,205 hashes including each initial state. All 288 selected stills passed; 26 full/reduced clean/debug comparison pairs differ per party. Every movie reaches the expected final state from tick zero. Audible cue counts are identical across the 16 modes within each party, with no rejected cue. The highest raw/transcoded stereo peak is −6.779081 dBFS. Music is enabled only in the requested modes, with two phase changes when enabled and none when muted.

| Players | Accepted ticks | Movies | Audible cues per movie | Maximum SFX voices | Movie durations, seconds |
| --- | ---: | ---: | ---: | ---: | ---: |
| Solo | 2,949 | 16 | 583 | 11 | 50.733–201.283 |
| Two | 2,957 | 16 | 942 | 16 | 50.904–201.825 |
| Four | 3,296 | 16 | 1,888 | 25 | 56.573–224.496 |

A completed solo `--resume` run repeated the replay/still checks and verified/reused all 16 movies without recording again. Final `CHECKPOINT_DISABLE=1 pnpm check` passed 694 tests across 69 files, including asset, art, audio, content, type, bundle and infrastructure checks. The [Linux clock probe](W04-host-clocks.md) separately retains the observed host discrepancy; these captures do not establish stable-host or deployed timing acceptance.

The current weapons comparison uses accepted right-facing horizontal sidearm, HMG, shotgun, grenade and flamethrower actions at ticks 602, 657, 810, 702 and 1,348 respectively. Each column shows its release and two/four/six ticks later, with the operative root aligned and effects limited to that action instance. This removes unrelated earlier blasts and tracers while preserving the actual world, actor poses and source pixels. Six native/enlarged black/white/chroma sheets retain all 20 accepted state hashes; they are explicitly source compositions rather than live browser screenshots.

The package includes an offline `review.html` viewer with five movie selectors, nine scene jumps per party and the current weapons comparison. Download the assets into one directory and open that page in a browser. The viewer restarts the chosen recording from its beginning and provides direct MP4/raw WebM links. Scene jumps pause about one second before the selected accepted event; they do not assert frame-exact media seeking. Chromium checked all 48 selections and native 384×216 movie dimensions, all 27 scene jumps, advancing muted playback, image loading and a 400-pixel viewport without document overflow. Desktop viewing uses integer 2× movie scaling. The desktop/mobile layouts were also visually inspected. The downloaded set subsequently passed all 48 selections and 27 scene jumps directly through a `file:` URL, with no page errors or network requests; that report and its screenshots are retained in the delivery receipt.

All eight W06 human review categories remain pending: silhouette/readability, pixel discipline, motion/acting, weapon distinction, vehicle weight, environment separation, multiplayer clarity and audio impact. The current native/enlarged operative and tank sheets remain linked from the [review index](style-v2/README.md). Physical-device output, human listening/style approval, production v3 and the remaining campaign remain open. The [live observability check](../observability.md) found no `edgefall` Worker in the authenticated account, so local tracing configuration is not live ingestion proof.
